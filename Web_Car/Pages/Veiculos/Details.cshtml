@page "{id:int}"
@model Web_Car.Pages.Veiculos.DetailsModel
@{
    ViewData["Title"] = "Detalhes do Veículo";
}

<h2>Detalhes do Veículo</h2>

<div>
    <strong>Nome:</strong> @Model.Veiculo.Nome
</div>
<div>
    <strong>Modelo:</strong> @Model.Veiculo.Modelo
</div>
<div>
    <strong>Marca:</strong> @Model.Veiculo.Marca
</div>
<div>
    <strong>Renavam:</strong> @Model.Veiculo.Renavam
</div>
<div>
    <strong>Ano de Fabricação:</strong> @Model.Veiculo.AnoFabricacao
</div>
<div>
    <strong>Ano do Modelo:</strong> @Model.Veiculo.AnoModelo
</div>
@if (!string.IsNullOrEmpty(Model.Veiculo.FotoPath))
{
    <div>
        <strong
::contentReference[oaicite:0]{index=0}
 
@if (!string.IsNullOrEmpty(Model.Veiculo.FotoPath))
{
    <div>
        <img src="@Model.Veiculo.FotoPath" alt="Foto do Veículo" style="max-width:300px;" />
    </div>
}

<p>
    <a asp-page="Index">Voltar para a lista</a> |
    <a asp-page="Edit" asp-route-id="@Model.Veiculo.Id">Editar</a> |
    <a asp-page="Delete" asp-route-id="@Model.Veiculo.Id">Excluir</a>
</p>
